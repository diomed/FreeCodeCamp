var myRequest = new XMLHttpRequest();
var url = "http://ip-api.com/json";

myRequest.open('GET', url, false);
httpRequest.onload = function() {
    console.log(httpRequest.responseText);
};

httpRequest.send();

    
/*var city, lon, lat;

$.getJSON("http://ip-api.com/json", function(response) {
    city = response.city;
    console.log(city);
}).done(function() {
    alert("Success");
}).fail(function(d, textStatus, error) {
    console.error("get JSON failed, status: [" + textStatus +"], error: [" + error + "]");
});

    if (location.status === "success") {
      var weather = $.getJSON("api.openweathermap.org/data/2.5/weather?lat=" + location.lat + "&lon=" + location.lon + "&APPID=0feb9cfecdf5300b1cc74126d5314767", function(response) {
      return response;
      });

      $("#location").html(weather.regionName + ", " + weather.country);
      } else
      alert("Could not get your location.");*/
